{
  "name": "pi-ainsel-runner",
  "version": "0.1.0",
  "description": "Pi extension: the ainsel agent runtime event loop. Polls the hub for tasks, drives pi via sendUserMessage per event, ACKs/NACKs via REST.",
  "main": "index.ts",
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "test": "node --experimental-strip-types --test index.test.ts"
  },
  "dependencies": {
    "prom-client": "^15.1.3"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
